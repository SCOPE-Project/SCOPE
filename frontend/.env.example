# No environment variables are currently required by the frontend.
# The mission map now renders its own bundled world outlines
# (public/world/countries-110m.json) instead of an external tile style.
